const isValidId = require('./isValidId');
const validate = require('./validate');
const authorizate = require('./authorizate');
const upload = require('./upload');

module.exports = { isValidId, validate, authorizate, upload };
